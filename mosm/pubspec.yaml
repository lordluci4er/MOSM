name: mosm
description: "Medical Order & Stock Manager App 🚀"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.3.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🌐 API Calls (UPDATED ✅)
  http: ^1.2.1

  # 💾 Local Storage
  shared_preferences: ^2.2.2

  # 🔔 Firebase (optional, keep if using)
  firebase_core: ^2.30.0
  firebase_messaging: ^14.7.10

  # 🎨 Fonts
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true